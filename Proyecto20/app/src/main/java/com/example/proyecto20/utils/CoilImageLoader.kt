package com.example.proyecto20.utils

object CoilImageLoader {
    // Este objeto se mantiene para compatibilidad, pero ya no tiene funcionalidad específica de ExerciseDB
    // Las imágenes se cargarán usando el ImageLoader estándar de Coil
}

